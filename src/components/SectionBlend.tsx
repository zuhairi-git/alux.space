// SectionBlend removed — replaced by SectionAccents for per-section decoration.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function SectionBlend(_props?: { from?: string; to?: string }) {
  return null;
}
